<h1><%= @room.number %></h1>

<p><%= @room.room_type %></p>

<ul>
  <li><%= link_to "Edit", edit_room_path(@room) %></li>
  <li><%= link_to "Destroy", room_path(@room), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>

<h2>Bookings</h2>
<%= render @room.bookings %>

<h2>Add a booking:</h2>
<%= render "bookings/form", room: @room %>